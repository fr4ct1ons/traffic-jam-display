#pragma once

// Duplicate this file, rename it to "secrets.h" and overwrite these defines with your WiFi SSID and password and MQTT credentials.
#define WIFI_SSID "myssid"
#define WIFI_PASSWORD "mypass"

const char* mqttServer = "homeassistant.local";
const char* mqttUser = "user";
const char* mqttPassword = "password";